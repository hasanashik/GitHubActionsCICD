name: Learn tf CI
run-name: ${{ github.actor }} is learning tf CI

on:
  push:
    branches:
      - main

jobs:
  install-make:
    runs-on: ubuntu-latest

    steps:
      - shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install make 
          make --version
